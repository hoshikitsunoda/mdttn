export interface WindowSize {
  mobile: string
  laptop: string
}

export interface Theme {
  colors: {
    text: string
    secondaryText: string
  }
  device: {
    mobile: string
    laptop: string
  }
}
